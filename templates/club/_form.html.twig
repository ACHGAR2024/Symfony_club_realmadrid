{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="text-right">
    <button class="btn bg-primary text-white">{{ button_label|default('Sauvegarder') }}</button>
</div>
{{ form_end(form) }}
