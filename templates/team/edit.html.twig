{% extends 'dashboard.html.twig' %}

{% block title %}Gestion des Sections{% endblock %}

{% block content %}
    
        
            <div class="col-md-6">
                <h1 class="bg-dark text-white p-3 rounded">Modifier une équipe</h1>
                <img src="{{ asset('uploads/images/' ~ team.photo) }}" width="100" class="rounded m-3">
          {{ include('team/_form.html.twig', {'button_label': 'Mettre à jour'}) }}    
        
                <a href="{{ path('app_team_index') }}" class="btn btn-dark mt-2 mb-2">Retour à la liste</a>
                {{ include('team/_delete_form.html.twig') }}
            </div>
      
   
{% endblock %}

